/* Tailwind CSS + Basecoat entrypoint.
 *
 * Tailwind utilities get compiled into public/assets/styles/output.css using the
 * standalone CLI (see Makefile). Basecoat core styles are provided via CDN in
 * the generated templates so you can extend or override them here as needed.
 */
@import "tailwindcss";

/* Add custom component overrides below. */
