/*
 * DaisyUI output is generated by the Tailwind CLI installed via:
 *   curl -sL daisyui.com/fast | bash -s -- ./web/assets/styles
 *
 * Run `make go` to bootstrap the CLI (first run only) and stream updates with
 * `--watch`. The watcher writes to public/assets/styles/output.css.
 */

/* Tailwind will overwrite this file. */
